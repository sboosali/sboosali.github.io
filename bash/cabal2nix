#!/bin/bash

# -*- mode: sh; -*-

set -e
set -u

##################################################

(cd ./hs  &&  cabal2nix --shell ./. > ./shell.nix)

##################################################

cat hs/shell.nix

##################################################