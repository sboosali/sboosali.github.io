##################################################
{ nixpkgs ? <nixpkgs>
, pkgs    ? (import nixpkgs {}).pkgs
}:

##################################################
# Imports ########################################
##################################################
let
#------------------------------------------------#

environment = import ./nix/environment.nix
  {
    inherit (pkgs) stdenv nodePackages;
    systemPackages = pkgs;
  };

#------------------------------------------------#
in
##################################################

environment

##################################################